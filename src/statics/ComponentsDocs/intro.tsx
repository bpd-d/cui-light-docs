import { DocsScript } from "../../components/docs/base"

export const Introduction: DocsScript = {
    sections: [{
        name: "Usage",
        description: `Components allow to create user interfaces and interactions that look beautiful and consistent across whole application.
        The idea behind is to create environment for developers who don't want create components library from scratch or attach to any popular front-end framework.
        cUI library and components will work with all latest front-end frameworks. Some of the components are written in pure CSS, so they mostly cover the visual part. 
        These components which require special behavior provide logic written in Javascript. Each custom JS component use own special attribute to get setup for it. 
        `
    }]
}
