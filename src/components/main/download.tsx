import * as React from "react";
import { PageBase } from "../base/PageBase";
import { PageWithHeaderBase } from "../base/PageWithHeaderBase";

export default function Download(props: any) {
    React.useEffect(() => {
    })
    return (
        <PageWithHeaderBase name="Download" description="Download or import files to your project"></PageWithHeaderBase>
    )
}